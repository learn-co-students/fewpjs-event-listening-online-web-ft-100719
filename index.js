const input = document.getElementById('input')

function addingEventListener() {
    input.addEventListener('click', function addingEventListener(event) {
        alert('Oof, don\'t click so hard');
    })
}